version: '3.9'

services:
  db:
    image: postgres:16.2-alpine3.18
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=alef2024
    ports:
      - '5433:5433'
    volumes:
      - db:/var/lib/postgresql/data
volumes:
  db: